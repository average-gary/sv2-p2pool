//! Cap'n Proto client for talking to a p2poolv2 IPC daemon (Phase 2).
//!
//! This crate materializes once an IPC server lands upstream in p2poolv2.
//! Phase 1 uses in-process embedding via `sv2-p2pool-engine` directly.

#![forbid(unsafe_code)]
